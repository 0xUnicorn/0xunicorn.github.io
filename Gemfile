source "https://rubygems.org"

# GitHub Pages dependencies (locked to GitHub’s Jekyll version)
gem 'github-pages', '~> 232', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-seo-tag'
end

gem "webrick"
